# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Данные приложения

### Описание типов данных

#### TPayment

Тип для выбора способа оплаты.

```typescript
type TPayment = "card" | "cash";
```

#### IProduct

Интерфейс для описания товара в каталоге.

Поля:

- `id: string` — уникальный идентификатор товара
- `description: string` — описание товара
- `image: string` — путь к изображению товара
- `title: string` — название товара
- `category: string` — категория товара
- `price: number | null` — цена товара (null если товар недоступен)

#### IBuyer

Интерфейс для описания данных покупателя при оформлении заказа.

Поля:

- `payment: TPayment` — способ оплаты ("card" или "cash")
- `email: string` — электронная почта покупателя
- `phone: string` — номер телефона покупателя
- `address: string` — адрес доставки

#### IProductsResponse

Интерфейс для ответа сервера при получении каталога товаров.

Поля:

- `total: number` — общее количество товаров в каталоге
- `items: IProduct[]` — массив товаров

#### IOrder

Интерфейс для данных заказа, отправляемых на сервер при оформлении покупки.

Поля:

- `payment: TPayment` — выбранный способ оплаты
- `email: string` — email покупателя
- `phone: string` — телефон покупателя
- `address: string` — адрес доставки
- `total: number` — общая стоимость заказа
- `items: string[]` — массив ID товаров в заказе

#### IOrderResponse

Интерфейс для ответа сервера при успешном оформлении заказа.

Поля:

- `orderId: string` — уникальный ID оформленного заказа
- `total: number` — общая сумма заказа

---

## Модели данных

Модели данных отвечают за хранение и управление данными приложения. Они полностью независимы от других компонентов и не содержат логики отображения.

### Класс Products (Каталог товаров)

**Назначение:** Хранение всех доступных товаров в каталоге и управление выбранным товаром для подробного просмотра.

**Файл:** `src/components/models/products.ts`

**Конструктор:**

- Параметры: нет
- Инициализирует пустой массив товаров и null для выбранного товара

**Поля:**

- `items: IProduct[]` — массив всех доступных товаров в каталоге
- `selectedItem: IProduct | null` — товар, выбранный для детального просмотра (null если товар не выбран)

**Методы:**

- `setItems(items: IProduct[]): void` — сохранить массив товаров в каталог
- `getItems(): IProduct[]` — получить все товары из каталога
- `getProductById(id: string): IProduct | null` — найти товар по его ID (возвращает null если товар не найден)
- `setSelectedProduct(item: IProduct): void` — выбрать товар для подробного просмотра
- `getSelectedProduct(): IProduct | null` — получить текущий выбранный товар (null если товар не выбран)

### Класс Cart (Корзина)

**Назначение:** Управление товарами, которые пользователь выбрал для покупки, расчет стоимости и количества.

**Файл:** `src/components/models/Cart.ts`

**Конструктор:**

- Параметры: нет
- Инициализирует пустую корзину

**Поля:**

- `items: IProduct[]` — массив товаров, находящихся в корзине

**Методы:**

- `addProduct(product: IProduct): void` — добавить товар в корзину (проверяет наличие товара и что цена не null)
- `removeProduct(productId: string): void` — удалить товар из корзины по его ID
- `getProducts(): IProduct[]` — получить все товары в корзине
- `getTotalPrice(): number` — получить общую стоимость всех товаров в корзине
- `getItemCount(): number` — получить количество товаров в корзине
- `isProductInCart(productId: string): boolean` — проверить находится ли товар в корзине
- `clearCart(): void` — очистить корзину полностью

### Класс Order (Данные покупателя и заказа)

**Назначение:** Хранение и управление данными покупателя при оформлении заказа, включая валидацию данных.

**Файл:** `src/components/models/Order.ts`

**Конструктор:**

- Параметры: нет
- Инициализирует пустые данные покупателя

**Поля:**

- `data: IBuyer` — объект с данными покупателя (payment, email, phone, address)

**Методы:**

- `setOrderData(data: Partial<IBuyer>): void` — сохранить или обновить данные покупателя (использует Partial для частичного обновления без затирания других полей)
- `getOrderData(): IBuyer` — получить все сохраненные данные покупателя
- `clearOrderData(): void` — очистить все данные покупателя (сбросить в пустые значения)
- `validateOrderData(): { [key: string]: string }` — валидировать данные заказа, возвращает объект с ошибками (пустой объект если ошибок нет). Проверяет валидность email, телефона, адреса и выбор способа оплаты.

---

## Слой коммуникации

Слой коммуникации отвечает за взаимодействие приложения с API сервера.

### Класс Server

**Назначение:** Выполнение запросов к API сервера для получения товаров и отправки заказов.

**Файл:** `src/components/base/Server.ts`

**Конструктор:**

- Параметры: `api: IApi` — экземпляр класса Api для выполнения HTTP запросов
- Сохраняет переданный экземпляр API для использования в методах класса


**Методы:**

- `async getProducts(): Promise<IProduct[]>`
  - Выполняет GET запрос на эндпоинт `/product/`
  - Возвращает промис с массивом товаров
  - В случае ошибки выводит сообщение об ошибке в консоль и пробрасывает ошибку дальше

- `async createOrder(order: IOrder): Promise<IOrderResponse>`
  - Выполняет POST запрос на эндпоинт `/order/`
  - Отправляет данные заказа на сервер
  - Возвращает промис с ответом сервера (ID и сумма заказа)
  - В случае ошибки выводит сообщение об ошибке в консоль и пробрасывает ошибку дальше

---

## Presenter (Связующий слой)

### Класс Presenter

**Назначение:**  
Presenter является центральным связующим звеном приложения. Он управляет бизнес-логикой, обрабатывает события от View и моделей, координирует обновление интерфейса и данных.

**Файл:** `src/components/presenter/Presenter.ts`

Presenter реализует архитектурный паттерн MVP и обеспечивает слабую связанность между слоями Model и View за счёт использования событий.

**Конструктор:**

Принимает экземпляры моделей, представлений и брокера событий:

- `products: Products` — модель каталога товаров  
- `cart: Cart` — модель корзины  
- `order: Order` — модель данных заказа  
- `events: EventEmitter` — брокер событий  
- `gallery: Gallery` — представление каталога  
- `modal: Modal` — модальное окно  
- `basket: Basket` — представление корзины  
- `header: Header` — шапка сайта  

В конструкторе происходит инициализация и подписка на все события приложения.

---

### Обработка событий

Presenter подписывается на события, генерируемые моделями и представлениями, и выполняет соответствующую бизнес-логику:

- **`products:loaded`** — сохраняет товары в модель `Products` и передаёт их в `Gallery` для отображения  
- **`card:select`** — выбирает товар по ID и открывает модальное окно предпросмотра  
- **`basket:add`** — добавляет товар в корзину и обновляет счётчик  
- **`basket:remove`** — удаляет товар из корзины и обновляет отображение  
- **`basket:open`** — открывает корзину в модальном окне  
- **`order:open`** — открывает форму оформления заказа  
- **`form:submit`** — сохраняет данные заказа и открывает форму контактов  
- **`order:contact`** — сохраняет контактные данные и отправляет заказ  
- **`success:close`** — закрывает модальное окно и очищает корзину  

---

## Слой представления (View)

Слой View отвечает исключительно за отображение данных и пользовательское взаимодействие. Компоненты представления не содержат бизнес-логики и взаимодействуют с системой через события.

### Gallery (Каталог товаров)

**Файл:** `src/components/view/Gallery.ts`

**Назначение:**  
Отображение списка товаров на главной странице.

**Методы:**

- `display(products: IProduct[]): HTMLElement` — очищает контейнер и отрисовывает карточки товаров

---

### Card (Базовая карточка товара)

**Файл:** `src/components/view/Card/Card.ts`

**Назначение:**  
Базовый класс карточки товара.

**Методы:**

- `setTitle(title: string): void`
- `setImageUrl(image: string, alt: string): void`
- `setPrice(price: number | null): void`
- `setCategory(category: string): void`

---

### CardCatalog

**Файл:** `src/components/view/Card/CardCatalog.ts`

**Назначение:**  
Карточка товара в каталоге. При клике инициирует событие выбора товара.

---

### CardPreview

**Файл:** `src/components/view/Card/CardPreview.ts`

**Назначение:**  
Карточка предпросмотра товара в модальном окне.

**Особенности:**
- Отображает подробную информацию о товаре
- Содержит кнопку добавления в корзину
- Блокирует кнопку, если товар уже добавлен в корзину

---

### Basket (Корзина)

**Файл:** `src/components/view/Basket.ts`

**Назначение:**  
Отображение товаров в корзине и общей стоимости заказа.

**Функции:**
- Отрисовка списка товаров
- Удаление товаров
- Переход к оформлению заказа

---

### Header (Шапка сайта)

**Файл:** `src/components/view/Header.ts`

**Назначение:**  
Отображение кнопки корзины и количества товаров.

---

### Modal (Модальное окно)

**Файл:** `src/components/view/Modal.ts`

**Назначение:**  
Универсальный компонент для отображения контента в модальном окне.

**Функции:**
- Открытие и закрытие модального окна
- Закрытие по клику на фон или кнопку

---

### Формы

#### Form (Базовая форма)

**Файл:** `src/components/view/Form/Form.ts`

**Назначение:**  
Базовый класс для всех форм приложения.

**Функции:**
- Сбор данных из input-полей
- Генерация событий отправки формы
- Отображение ошибок валидации

---

#### OrderForm

**Файл:** `src/components/view/Form/OrderForm.ts`

**Назначение:**  
Форма выбора способа оплаты и ввода адреса доставки.

---

#### ContactsForm

**Файл:** `src/components/view/Form/ContactsForm.ts`

**Назначение:**  
Форма ввода контактных данных пользователя (email и телефон).

---

### Success (Успешное оформление заказа)

**Файл:** `src/components/view/Success.ts`

**Назначение:**  
Отображение сообщения об успешном оформлении заказа и итоговой суммы.

---

## Точка входа приложения

### main.ts

**Файл:** `src/main.ts`

**Назначение:**  
Инициализация приложения и связывание всех компонентов.

**Последовательность работы:**
1. Создание экземпляра API и сервера
2. Создание брокера событий
3. Инициализация моделей данных
4. Инициализация компонентов представления
5. Создание Presenter
6. Загрузка каталога товаров и генерация события `products:loaded`

---

## Итог

Приложение представляет собой событийно-ориентированное SPA, реализованное по архитектуре MVP, что обеспечивает чёткое разделение ответственности, удобство поддержки и расширения проекта.
